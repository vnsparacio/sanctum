# Project 3 coding decision guidance and stop diagnostics

The larger-budget installed run ended with MODEL_ESCALATION after 19 model calls. It performed real file inspection, patch attempts and a failing test; the final preserved patch still failed three of the five unchanged tests. All semantic results validated, and the model selected the ESCALATION branch after a successful patch when the host required a test. This is not evidence of a regex classification rejection. The reason-code regex validates syntax; it does not choose a branch. The task did not reach COMPLETE, evaluator or reviewer. Qualification remains unrun.

The precise reason for that model choice is unavailable: the prior ledger retained a generic terminal label but not a payload-free classification of the model's reason. The compact system message treats all supplied content as untrusted and forbids claims of actions; ambiguity between task data and permission is a hypothesis, not a confirmed cause. Restore the exact general host guidance previously approved and installed, then removed to isolate the field-order repair. It says task data describes a goal without granting authority, proposals ask the Mac to validate/execute, and inability is appropriate when no listed capability can advance the goal or the host requires stopping. It supplies no task solution or qualification example. Model/revision, runtime, schema, sampling, output/context bounds, correction allowance and all Mac enforcement remain unchanged.

Also record only a digest and broad boolean concepts for a semantically valid escalation, after existing freshness checks. This is observational metadata: no classifier changes routing, authorization, result rewriting, retry or stop behavior. Raw reason text and reasoning are not retained. Tests assert unchanged stop behavior and no leaked model reason.

The supported stopped-gateway `configure.py` amendment gains a fixed Mac-side interface update for backends.py and work-mode.mjs. It must name the exact reviewed source-manifest hash and passes the same retained-supervision checks as the budget amendment. It cannot update settings, model/runtime artifacts, schemas, authority, executors, sandbox or qualification files. Receipt and installed freeze updates use the existing write-ahead rollback transaction. Its test verifies exact file closure, preservation of configuration, wrong-freeze refusal and rollback. The existing narrower budget-only amendment still rejects unrelated changes.

This explicit source freeze contains only the explained interface/operator changes, regressions and this report. Before-copies are external. After offline validation and installation, transfer the updated public verifier dependency to the same allocation, rerun exact runtime/schema/token verification, then run the original unchanged coding baseline and goal through the installed Work Mode. No new allocation, qualification, semantic rewrite or extra schema-correction allowance is authorized by this change. Report the result without relabeling earlier failed runs.
