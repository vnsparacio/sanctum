# V1.2 agent-system handoff

## Implemented and proven offline

- Central exact model routing with live catalog validation and no GPT-5.5 or
  hidden fallback.
- Immutable management authority boundaries and the exact Ready for Agent plus
  `symphony` eligibility gate.
- External private runtime, stable run IDs, structured logs, role locks, stale
  recovery, dry-run/shadow labeling, and workload budgets.
- Repo Steward incremental/deep evidence, host-validated findings,
  deduplication, one real Terra shadow, and typed Engineering Finding payloads.
- Product Scout native bounded web research, public-source validation,
  deduplication, one real Terra shadow, and typed Product Discovery payloads.
- Triage bounded snapshot input, Luna/Terra routing, management-only state
  transitions, duplicate linking semantics, owner briefings, and a real Luna
  fixture shadow.
- Symphony execution workflow with Sol-medium, deterministic branches,
  proportional validation, private workpads, unmerged PRs, and Human Review.
- A Sanctum-owned outer supervisor enforcing aggregate elapsed time, turns,
  tokens, retries, stalls, state-API health, and output with process-group kill
  plus private incident receipts.
- Independent Sol-high reviewer with strict Human Review/unmerged/base/branch
  packet validation and one real shadow review.
- Qualified Linear metadata and idempotent/capped payload builders tested
  against synthetic metadata; existing full CI plus a focused agent-contract
  step.
- Version-controlled disabled schedule plan and exact Codex desktop setup
  instructions.

## Live Linear qualification and remaining implementation checkpoint

The existing Sanctum V1.2 project and owning team have now been observed live.
The project slug, required workflow states, issue labels, `source` label group,
and all three issue templates match the reviewed metadata contract. The
IDs-only snapshot is private, mode `0600`, outside Git. The owner established
`LINEAR_API_KEY` only in the local process environment; it is not stored in
source, prompts, issues, or logs.

Repo Steward created one capped Engineering Finding (`TTE-7`) in Triage and a
replay resolved to that issue as a duplicate. Product Scout created one capped
Product Discovery (`TTE-8`) in Triage and its replay also resolved as a
duplicate. Triage captured an authenticated, management-only live snapshot,
moved `TTE-7` to Backlog and `TTE-8` to Watch, and did not alter labels or touch
either implementation gate. The two Repo Steward schedules, Product Scout
schedule, and Triage schedule are active in the local Codex project. Management
creation remains capped at one issue per run for the initial live period.

The implementation lifecycle remains unqualified live. The reviewed, clean
local Symphony checkout is at commit
`be10a1b79df723d6d7612b5651c8522704dafb2e`. Sanctum preflight passes when it is
given that local binary and an external workspace root; the current process
does not persist either path. The owner must create or choose the tiny
documentation issue and personally set both `Ready for Agent` and `symphony`
before any start. No management agent may set either gate. The implementation
supervisor's local fake-service kill/incident smoke remains valid but is not
represented as live lifecycle proof.

## Deliberately deferred

Splunk, Galileo, HEC, telemetry pipelines, Splunk MCP, and an Observability
Steward are out of scope. The extension seam is the stable JSONL run log,
incident receipt, and role/run identity. No current feature depends on Splunk.

Automatic merge and automatic Done transitions are also deferred by policy,
not by implementation incompleteness. The human owner remains the final merge
authority.

## Repository and runtime boundaries

The legacy historical repository and the external Symphony source were not
modified. The private VinceAI runtime was not amended. Generated
agent logs, state, locks, shadow artifacts, and incidents remain outside Git.

The feature branch must be reviewed by PR into `v1.2-dev`; do not merge it
automatically and do not begin a sibling V1.2 project from this branch.
