# V1.3 Qwen lifecycle qualification

Date: 2026-09-22. Linear control issue
[TTE-81](https://linear.app/ttercode/issue/TTE-81/qualify-one-non-sanctum-qwen-to-pr-lifecycle-under-owner-gate)
is qualified by the owner-gated child issue
[TTE-90](https://linear.app/ttercode/issue/TTE-90/qualify-qwen-by-extending-slugify-in-the-non-sanctum-fixture)
and its exact
[candidate pull request](https://github.com/vnsparacio/sanctum-work-mode-qualification/pull/1).
This is a bounded lifecycle result, not general Qwen coding-quality evidence or
permission to enable continuous implementation.

## Qualified path

The owner selected the reviewed `v13-qualification` profile, which fixes the
non-Sanctum repository to `vnsparacio/sanctum-work-mode-qualification`, the
integration branch to `main`, and host validation to `build`, `lint`, and
`test`. TTE-90 entered execution only after the owner applied `Ready for Agent`,
`symphony`, and exactly one routing label. Symphony then moved it to
`In Progress`; issue text and model output did not select the repository,
backend, commands, credentials, or Git destination.

Qwen implemented the bounded `slugify` change on `symphony/tte-90`. The task
completed as `COMPLETE / HOST_VALIDATED_FINAL` after four iterations and six
model calls. Protected-input integrity passed before evaluation, during
evaluation, and before completion. The host evaluator passed, and the separate
data-only Work Mode reviewer returned `ACCEPT`.

The delivered candidate was commit
`fca7840191a689e20fe22e780ddf927d1983ae4b`. It changed only `README.md`,
`src/slugify.js`, and `test/slugify-punctuation.test.js`. Host validation passed
`npm run build`, `npm run lint`, `npm test` with five tests, and
`git diff --check`. The GitHub `test` check also passed. The task workpad
records the profile, Qwen task ID `64d3e6ec9b17c3d01c392a0ef34d11da`,
validation, changed files, commit, PR, and advisory verdict.

## Authority stop and later owner action

The worker attached PR #1 to TTE-90 and moved the issue to `Human Review` at
2026-09-23 01:09:23 UTC. Its final workpad recorded the PR as open, non-draft,
mergeable, green, and unmerged. No worker merge or Done transition occurred.
The final private GPU state was `OFFLINE` with zero leases and zero active
requests.

The human owner later merged PR #1 at 2026-09-23 01:35:23 UTC as GitHub user
`vnsparacio`, producing merge commit
`64469713bf01dd95b2ac7f1cac813ebd1c638265`, and moved TTE-90 to `Done` at
2026-09-23 01:35:25 UTC. Those later owner actions are distinct from the
qualified automated lifecycle and demonstrate that its Human Review stop was
preserved.

## Retry and stop evidence

Earlier TTE-81 attempts stopped with `OWNER_ACTION_REQUIRED` because the
non-Sanctum target, reviewed Work Mode workflows, backend binding, and
app-server path were absent. The worker did not substitute the Sanctum
repository, manufacture an authority-bearing target, or retry the unchanged
deterministic mismatch. The then-active tracker state caused 20 supervisor
continuations despite the durable blocker; the accepted V1.3 blocker-loop and
operator-blocker changes now make that condition terminal for the supervisor
invocation.

The first target run then exposed two concrete prerequisites. Process-local
Work Mode task capabilities could not be forwarded through the general
gateway, and Docker Desktop was stopped. The reviewed app-server adapter keeps
the capability binding in its owning process and fails fast on exact
runner-image readiness before model allocation. The successful TTE-90 run
occurred only after those prerequisites were corrected. There was no silent
fallback, alternate repository, blind consequential replay, or relaxation of
validation.

## Result and limits

This evidence satisfies the TTE-81 slice:

- one owner-authorized non-Sanctum Qwen implementation reached a host-validated
  commit, normal push, pull request, independent advisory review, and Human
  Review stop;
- the exact candidate PR and Linear workpad are linked;
- deterministic blockers, retry behavior, cleanup, and observed limitations
  are recorded; and
- merge and Done authority remained with the owner.

The qualification covers one small JavaScript behavior change in one synthetic
fixture repository. It does not establish broad implementation quality,
provider availability, arbitrary repository support, or unattended operation.
The advisory review is recorded in the Linear workpad rather than as a GitHub
review object. The source defaults remain `implementation_backend: codex` and
implementation `write_enabled: false`; changing either is a separate reviewed
owner decision.
