# V1.2 Linear live qualification

Qualification date: 2026-09-19.

## Qualified metadata

- Project: `Sanctum V1.2`
- Slug: `sanctum-v12-6fe3a63e0c69`
- Project ID: `2d0de4b3-e2ec-4365-a41e-27063161d44e`
- Owning team: `Ttercode` (`c8fcb9c4-1a3c-4166-9972-07e57f424edb`)
- The nine required workflow states, eleven required labels, `source` label
  group, and three required templates match the reviewed fixture names.

The IDs-only runtime snapshot is
`$SANCTUM_AGENT_PREFIX/state/linear-metadata.json`, mode `0600`. Authentication
remains in the owner-controlled process environment and is not persisted in
source or runtime artifacts.

## Live management evidence

- Repo Steward ran with a one-issue creation cap and created `TTE-7` in Triage
  with `tech-debt`, `reliability`, `agent-quality`, and `Repo Steward`.
- Replaying the same typed proposal returned `duplicate` for `TTE-7` and made no
  second issue.
- Product Scout ran with a one-issue creation cap and created `TTE-8` in Triage
  with `product-discovery`, `architecture`, `agent-quality`, and
  `Product Scout`; remaining proposals stopped at the creation cap.
- Replaying the same typed proposal returned `duplicate` for `TTE-8` and made
  no second issue.
- The authenticated Triage provider captured only management states and issues
  with exactly one approved source label into a private `0600` snapshot.
- A real Triage run moved `TTE-7` to Backlog and `TTE-8` to Watch. It did not
  add labels, move either issue to Ready for Agent, or authorize implementation.

All three management roles preserve their immutable authority boundary:
Repo Steward, Product Scout, and Triage cannot add `symphony`, cannot move work
to `Ready for Agent`, cannot modify source, and cannot merge.

## Active schedules

- Repo Steward incremental: weekdays at 08:15 America/Los_Angeles.
- Repo Steward deep: Saturday at 09:00 America/Los_Angeles.
- Product Scout: Monday, Wednesday, and Friday at 08:45 America/Los_Angeles.
- Triage: daily at 09:30 America/Los_Angeles.

The schedules execute in the saved local Sanctum project, stay quiet on
non-actionable success, and notify only for failures, budget stops, candidate
artifacts, management changes needing review, or required owner action.

## Subsequent implementation qualification

This qualification originally covered only the Linear management path. The
owner later set both execution gates on controlled documentation issue `TTE-46`.
Using the reviewed Symphony checkout
`be10a1b79df723d6d7612b5651c8522704dafb2e`, the bounded Git control plane created
the deterministic branch, commit, push and documentation-only PR #14 against
`v1.2-dev`, then stopped at Human Review. The owner reviewed and merged that PR
separately; PR #15 integrated the control plane itself.

That subsequent evidence does not change the management boundary above and does
not pre-authorize future work. Every future implementation still requires the
owner to set both `Ready for Agent` and `symphony`; only the owner may merge or
move work to Done. Binary paths, workspace roots and credentials remain outside
the repository.
