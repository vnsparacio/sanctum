# V1.1 live baseline — Project 0B checkpoint

**Status: implementation in progress; no new live baseline is accepted yet.** Historical V1 qualification does not establish this candidate's live acceptance.

## Source of truth

`/Users/tter/Projects/sanctum` is the development source of truth.
`https://github.com/vnsparacio/sanctum.git` is the canonical remote.
The private owner deployment lives outside Git.
`/Users/tter/Projects/hybrid-ai` is legacy reference/rollback only.
Future V1.1 projects build from the accepted Sanctum baseline.
Private state/secrets are never copied into source.

The exact accepted source baseline is tag `v1.0.0`, commit `83a1edf02c097e75915bd1c4f233963fe26388b7`. Development branch `v1.1-dev` starts at that commit. Project 0B checkpoint commits follow it without rewriting V1 history. The final handoff records the exact checkpoint commit after commit creation; no push is authorized.

The linked Project 0A execution was interrupted before producing a report. The owner authorized reconstruction from actual source and legacy configuration. The detailed migration manifest and private execution receipts stay outside this repository.

## Verified at this checkpoint

- Canonical remote fetched; V1 tag and original main resolve to the accepted source commit.
- Dependency bootstrap and six canonical plugin builds passed with unchanged runtime pins.
- All 152 packaged tests passed; source audit scanned 178 original files with zero issues before private setup.
- The first test harness used umask 077, which made the deliberately broad-permission fixture private. It failed the expected-rejection assertion. Rerunning with the ordinary 022 test environment passed; no source/hash change was used to suppress that failure.
- External private setup and doctor passed. Setup generated fresh gateway authentication and gate authority; no old sessions/approvals were copied.
- Private contacts, account identifiers, file roots and GPU resource references were applied using supported amendments and private rollback receipts. Personal-source tools and GPU autostart remain disabled pending their acceptance stages.
- Candidate Qwen 4B identity, direct arithmetic and unauthenticated gateway rejection passed, with no model-weight copies under the prefix. Two authenticated gateway arithmetic checks returned incorrect two-digit answers (including the canonical gate adapter path). This is an unresolved live-answer failure, not a passed gateway acceptance gate.

## Acceptance remaining

Complete 4B inference/restart/stop, authenticated OpenClaw/tool loop, saved owner WebUI chat and gate disclosure/boundary checks, public search/fetch, Browser Guard, bounded Gmail/Messages/Calendar, synthetic File Steward/Markdown, curated MCP transports, synthetic hosted adapters, deliberate private-80B lifecycle and cleanup, and final source separation/fresh-build verification. Owner enrollment and permission checks must occur locally. Never include personal source contents in evidence.

Legacy search/fetch and MCP transport wiring are absent from the generated candidate default and are not supported by the current amendment schema. Candidate Markdown uses prefix-owned notes instead of the legacy notes destination. These are migration gaps, not accepted parity. Resolve with reviewed generic configuration tooling before claiming completion; do not copy the old OpenClaw configuration wholesale or expose generic execution.

The [V1 qualification](qualification.md) retains browser-target selection and draft-format limitations plus the [development-only dependency advisory](dependency-review.md). No new live test is implied by those historical results. The package does not establish general factual correctness or unattended production cutover.

## Build, audit and deployment expectations

Run `make deps`, `make build`, `make test`, and `make audit`. Use an ordinary test file-creation mask (022); retain private logs separately with owner-only access. Inspect an existing `.venv` before attempting dependency bootstrap again. Do not upgrade dependencies to clear an advisory as part of migration.

Use `make setup PREFIX=/absolute/private/prefix` only on an absent/empty prefix or a matching verified install, then `make doctor PREFIX=/absolute/private/prefix`. Doctor verifies source, runtime pins and rendered configuration; it does not prove live integrations. Supported amendments require a stopped gateway and generate private rollback transactions.

The publication regression also exposed an existing scanner gap: the example-home exception matched example-prefixed real account names. The exception now ends at a slash or end of text. Only the two exact source/legacy declaration lines above are approved host-path documentation exceptions in the publication scanner. Other owner-home bindings remain rejected. Never allowlist private runtime paths, account data or secrets.

## Rollback

Follow [migration and rollback](migration.md). End candidate gate sessions and use its managed GPU stop. Verify provider-side zero owned compute, no leases and no uncertain allocation before stopping cleanup. Keep persistent model storage and private receipts intact.

Stop the candidate gateway with `make down PREFIX=/absolute/private/prefix`; stop only verified candidate foreground model/WebUI processes. Restore only matching configuration amendments via `scripts/configure.py --rollback`, with the candidate gateway stopped. Legacy configuration and services were not replaced; do not reset or copy entire legacy databases. Preserve all source commits and the V1 tag; ordinary rollback does not require a force reset or push.
