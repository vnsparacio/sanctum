# V1.1 live baseline — Project 0B

**Project 0B implementation complete.** This is an isolated, owner-enrolled candidate with the bounded acceptance below. It is ready for Project 0C review; Project 1 has not begun. This is not an unattended production cutover.

## Source of truth

`/Users/tter/Projects/sanctum` is the development source of truth.
`https://github.com/vnsparacio/sanctum.git` is the canonical remote.
The private owner deployment lives outside Git.
`/Users/tter/Projects/hybrid-ai` is legacy reference/rollback only.
Future V1.1 projects build from the accepted Sanctum baseline.
Private state/secrets are never copied into source.

Accepted implementation: branch `v1.1-dev`, commit `c7895cb620ecbdb1074f865453fac7462f1e0d68`. The documentation closeout is a descendant of that commit; the external handoff records its exact final HEAD. Qualified V1 remains tag `v1.0.0`, commit `83a1edf02c097e75915bd1c4f233963fe26388b7`. V1 history and the remote were not rewritten; nothing was pushed.

The referenced Project 0A execution had no completed report. The owner authorized reconstruction from the repositories. The itemized A/B/C migration manifest and private execution receipts remain outside source. A holds generic code/defaults; B holds private deployment bindings; C holds secrets and regenerated authority/state.

## Newly observed acceptance

- Before private setup: dependency bootstrap, six plugin builds, all 152 original packaged tests and publication audit passed. Reviewed core runtime pins remain unchanged. Final code passes all 158 packaged tests and the source audit (183 files, zero issues).
- Source-only export with independently bootstrapped dependencies passes build, all tests, audit, fresh setup and doctor. It does not require the owner deployment, accounts or credentials.
- Private setup and supported configuration amendments pass doctor. Gateway/authority credentials and session state are fresh. Owner WebUI enrollment, minimum-scope Google consent, Messages permission and exact one-use gate disclosure were completed locally.
- Cached Qwen 4B identity, direct inference, cache-only restart/stop and authenticated OpenClaw calculator execution pass. Only one heavy local model runs; there are no copied model weights in the candidate prefix.
- Saved WebUI chat passes the actual filter/pipe, Gemini disclosure and local tool answer. Reusing the consumed approval is refused. External WebUI connections, memory, automation and implicit feature paths are disabled; synthetic extra-tool/attachment/metadata requests are denied.
- Parallel public search and bounded public fetch pass with untrusted-content markers. Fetch used the normal readability path; a paid Firecrawl fallback request was not forced.
- Isolated Browser Guard navigation/snapshot and harmless native click pass, including denial, one-use approval, replay refusal, evaluation denial and profile isolation. The test tab/browser was closed.
- Gmail search/read, Messages history and Calendar events pass narrow read-only checks. Receipts contain status/counts, not personal source contents. No messages or email were sent; no calendar data was changed.
- Synthetic File Steward inventory/inspect/move/rename/undo and Markdown creation pass. Native approval tests include denial and replay refusal. Synthetic owner-directory files were removed after exact content verification; existing files were preserved.
- The isolated MCP profile exposes only time, offline Markdown conversion and public Hugging Face repository search. Transport, mount/path/network boundaries and disclosure/replay checks pass through native MCP clients and the actual guard.
- Synthetic hosted 235B and multimodal answers pass identity checks. The frontier adapter first returned a provider answer failure; a separate bounded diagnostic request subsequently passed the same identity/schema policy. No fallback or policy relaxation was introduced.
- One signed private-80B inference returned the expected answer within the 15-minute validation deadline. Invalid signatures were denied. The independent janitor ran successfully before allocation and after restart; provider-side zero compute, zero leases/active requests, preserved persistent volume and closed tunnel were confirmed. GPU autostart is off again.
- Separation scan covered 40,007 source/dependency/generated files: no known private secrets/bindings, private-prefix symlinks or unreviewed ignored/untracked files. Configuration and receipts remain private and outside Git.

## Intentional differences and limits

The candidate has isolated ports, auth stores, Google enrollment, browser state and MCP profile. It retains legacy model/provider policy and intentional contacts, file roots and notes destination through private bindings. No historical chats, pending approvals, nonces or undo journals were imported. Local MLX support dependencies were aligned with the observed legacy environment inside the private runtime; core source runtime pins were unchanged.

Unassisted 4B arithmetic returned incorrect answers in two gateway probes. Explicit calculator execution and the saved gate chat succeeded; this does not establish general model correctness. Keep exact work tool-backed. The frontier failure's cause is unresolved; successful retry evidence does not erase it.

File/Browser approval tests used the pinned native approval runtime. MCP used native clients and the actual guard. The headless gateway operator API failed closed when it lacked an approval route and did not expose MCP tools through that path. These tests do not claim full model-driven conversations for every integration, an interactive WebUI GPU conversation, or logout/reboot survival. Existing browser-target and draft-format limits remain in [V1 qualification](qualification.md).

Model, WebUI and personal brokers run under manual process supervision. Messages depends on the owner-authorized Terminal remaining open. Only the candidate GPU janitor has independently validated launchd supervision. Gateway restart requires a fresh `/gate new`; saved UI chat history does not restore in-memory gate authority. Production cutover and broader supervision remain separate work.

The [development-only dependency advisory](dependency-review.md) remains documented. No dependency upgrade was made merely to suppress it.

## Source changes and integrity

Generic amendments now support intentional notes destinations, pinned optional web plugins, a distinct GPU tunnel port and an isolated three-tool MCP profile. Both GPU backend and SSH tunnel honor the configured port. The janitor receives explicit local owner identity for Keychain access. Regression tests cover amendment rollback/rejection, transport authority drift, tunnel alignment and setup identity.

The publication scanner's example-home exception now ends at a slash or end of text. Only the two exact source/legacy declaration lines above are approved host-path documentation exceptions. Other owner-home bindings are rejected. Source freezes were explicitly updated for reviewed changes; no hash was changed just to hide unexplained drift.

## Build and deployment expectations

Run `make deps`, `make build`, `make test`, and `make audit`. Inspect an existing `.venv` before dependency bootstrap; do not recreate it blindly. Tests expect ordinary file-creation mask 022; private runtime/evidence uses owner-only access. An initial harness mask of 077 broke a permission-rejection fixture; the normal test environment passed without changing the assertion.

Use `make setup PREFIX=/absolute/private/prefix` only on an absent/empty prefix or matching verified installation. Run `make doctor PREFIX=/absolute/private/prefix` after setup and supported amendments. Doctor verifies source, core pins and rendered configuration; optional live health must be checked separately. Its generic optional-integration label is not an enrollment inventory. See [configuration](configuration.md) for web/MCP bootstrap and private amendments. Stop the candidate gateway before amendments.

## Rollback

Follow [migration and rollback](migration.md). End candidate gate sessions, perform managed GPU stop and verify provider-side zero ownership, leases and active requests before stopping cleanup. Preserve persistent model storage and private receipts. Never delete uncertain allocation state.

Stop the gateway with `make down PREFIX=/absolute/private/prefix`. Stop only candidate component processes after checking their recorded identity; use Ctrl-C for owner Terminal brokers. Retain private runtime data. Restore only a matching configuration transaction with `scripts/configure.py --prefix /absolute/private/prefix --rollback /absolute/private/transaction`, with the gateway stopped. Do not replace entire databases.

Legacy source/configuration/services were not replaced. If restoring a legacy model launcher, first stop the verified candidate model to prevent competing loaded servers. Preserve the V1 tag and all Project 0B commits; rollback needs no force reset or push. The external handoff contains exact local process/receipt locations and final source commit.
