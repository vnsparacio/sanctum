# Project 3 structured-output field-order repair

## Confirmed live diagnosis

The fourth separately authorized allocation passed exact runtime/compiler/token verification and one readiness call, then failed inspection with one valid ESCALATION. It made two known-complete model calls and no uncertain calls. The corrected operator retained that same allocation for non-inference diagnosis instead of cleaning up immediately.

On its actual pinned xgrammar compiler, the whole production ordinary schema accepted a canonical tool object beginning with `arguments`, but rejected the semantically identical tool object beginning with `kind`. It rejected the partial prefix selecting TOOL_PROPOSAL after `kind`, while accepting the equivalent ESCALATION prefix. This is a deterministic decoder asymmetry: alphabetic JSON serialization ordered tool fields as arguments, capability, kind, while escalation fields naturally began with kind. A model that chooses kind first has already excluded every tool branch. The prior exact verifier hid this problem by alphabetizing every representative as well as the schema. The precise unconstrained model preference is not observable from these receipts; the asymmetry itself is directly reproduced.

A non-inference prototype on the retained GPU ordered kind, capability, arguments and path first. All 25 natural-order representatives then accepted through EOS across all six whole generation schemas, without changing a schema value. An initial prototype that moved only kind/capability/arguments exposed the same ordering issue for optional numeric limits before path; the final prototype includes path before those limits. The actual serving vLLM request source uses ordinary `json.dumps(params.json)` for the compiler key, preserving schema insertion order.

Experiment `bd09cc1f7987af3a4c99f82ff4c0b2f9` was then deliberately ended because the failed semantic probe permits no further inference. Cleanup reached COMPLETE / CONFIRMED / ABSENT and provider inventory confirmed zero managed allocations. Creation-to-confirmed-absence was 518.217 seconds; conservative compute estimate USD 0.30085 at USD 2.09/hour, not a settled invoice. Receipt SHA-256: `4b112d11da16c2c81ee5c022d3cfb87e2afd5f070c99f4ced89ac0034723970a`. Prior exact-verification SHA-256: `d6b4a0aecbabf9d4db7107c184b372166594b9a06d4ad90c718892210756d68e`. Raw prompts, responses/reasoning and tool arguments were not retained in audit receipts. Storage remains preserved.

**LIVE MICROPROBE FAILED — EVIDENCE PRESERVED — PROJECT 3 REMAINS UNACCEPTED**

## Plan and implementation

Implement the demonstrated transport-order fix, test the actual outgoing bytes and compiler representative order, review/freeze the complete source, install through the supported stopped-gateway amendment, verify installed identity and health, then request fresh bounded live authorization. No extra inference, probe retry, replacement allocation or source/install mutation occurred on the retained diagnostic allocation.

Starting source-manifest SHA-256 is `56fda1d5b1eed492ae865eeec06e77340a2ff83bc2876f86e2ca08323bef9528`. The same Project 3 branch, HEAD, larger uncommitted tree and empty index remain preserved. Exact before-copies are external; legacy source is untouched. Changes in this freeze are:

- `gate/src/backends.py`: a deterministic generation-only JSON ordering helper places kind, capability, arguments and path before other keys; other keys are lexically ordered. Arrays, values and schema constraints are untouched. The production proposal schema and actual HTTP serialization use this order. Canonical signed authority, identities and digests retain their existing serializer. The speculative prompt guidance from the preceding repair is removed, restoring the original profile-plus-host prompt so the live change isolates the decoder fix.
- `gate/verify_exact_runtime.py`: compiles the same generation wire schema and feeds natural discriminator-first representatives through EOS, reporting the wire-schema SHA-256 separately from the unchanged semantic schema digest. This no longer treats alphabetized representative acceptance as sufficient proof.
- `gate/verify_serving_runtime.py`: backend selection and negative compiler controls use that same ordered schema.
- `gate/tests/test_protocol_repair.py`: verifies actual outgoing HTTP property order on every production surface, unchanged decoded schemas/canonical digests, path-first argument order, unchanged original prompt and untrusted-message boundaries, and unchanged valid result delivery.
- `gate/tests/test_prelive_targeted.py`: verifies the fresh compiler requests use the production wire serializer and bind its byte digest.
- This report and only those explained manifest entries.

The pinned Qwen profile, model revision, vLLM version, auto backend selection, quantization, thinking/sampling/output/context settings are unchanged. Generation/semantic schema values, Mac authority, result egress, sandbox, evaluator, correction allowance, budget and cleanup policies are unchanged. No model result is rewritten and no capability is forced. PRIVATE_80B remains permanently retired. The three fixed microprobes and expected outcomes are unchanged; no fixture examples or alternative planner are introduced.

## Review and validation

JSON property order is intentionally relevant only at the decoder boundary. The helper creates a new structure rather than mutating signed input. Its serialized output decodes to the identical schema; canonical hashes remain equal. Production HTTP, exact compiler input and representative encoding share the same helper. The pinned serving implementation preserves schema property order when deriving its compiler key. Host validation continues to accept semantically valid JSON independently of output property order.

Focused protocol (12), compiler orchestration (12) and serving-verifier (22) regressions pass. All **433 tests pass**: gate Node 111, gate Python 243, reliability Node 31, reliability Python 11, MCP 8, release Python 18 and plugin tests 11, with zero failures/errors/skips. Focused checks are not double-counted. The inspected cache-only dependency environment remains valid and no dependencies changed. `make build` passed all six plugin builds; `make audit` found zero issues in 295 files. The final production helper also passes all 25 natural-order representatives and invalid-object controls using the local pinned native compiler; both TOOL_PROPOSAL and ESCALATION discriminator prefixes are reachable. All 99 host negative controls still reject. Rendered input totals are again 1239, 4574, 4791, 9036, 858 and 535, with headroom 27433, 24098, 23881, 19636, 27814 and 28137, unchanged 4096 reserve and 32768 window. System content is restored to 77 tokens and overhead is 17. No implementation change followed the passing suite. Installed results are recorded separately. The tested local native compiler uses the observed pinned versions and tokenizer bytes, while live prototype evidence came from the actual retained serving allocation; these are recorded distinctly. No inference has tested the final packaged transport fix yet.

## Rollback and remaining gate

Before-copies allow selective source rollback; the supported amendment retains installed rollback state. Do not reset the larger tree or refresh hashes to conceal drift. Freeze source only after review and tests. Install with the candidate stopped and zero ownership, then check every overlay byte, freeze, receipt, doctor, authenticated help/status, retired refusal, janitor and installed experiment controls.

The prior allocation authorization is consumed. The next live run, if explicitly approved, uses one allocation at no more than USD 2.09/hour, at most 900 seconds and USD 0.75, one readiness call up to 16 tokens and five proposals up to 1024 tokens, only inspection → host-seeded post-patch test → inability. A failed verifier or semantic probe remains available for bounded non-inference diagnosis on that allocation. A failed semantic probe never resumes inference. Preserve the 120-second cleanup reserve. Real coding, qualification and independent acceptance remain separate later gates.
