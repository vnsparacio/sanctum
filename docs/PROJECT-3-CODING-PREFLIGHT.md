# Project 3 unseen coding test preparation

The owner requested proceeding to the coding test after the successful field-order live microprobe. This preparation registers one new private synthetic repository without modifying qualification fixtures or changing the model, prompt, runtime, execution policy, evaluator, reviewer, or existing budgets. The coding task and its patch are not qualification evidence.

The reviewed source change is limited to `scripts/configure.py`, its release tests, and this report. An explicit new source freeze covers these changes. The supported stopped-gateway configuration operator accepts a new `work_profile` registration with exactly a new name, an existing policy to copy, and a private repository within the owner prefix. It requires a clean committed repository and an existing private staging directory at the fixed registered-profile location. It refuses replacements, policy overrides, unknown profiles, source-tree repositories, symlinks, and dirty repositories. All execution settings are copied unchanged. The existing write-ahead receipt transaction and rollback cover the configuration file. Tests check preserved policy and rollback, plus rejection without mutation.

Before live work: validate the amended source, register the repository while the gateway is stopped, validate receipt and gateway health, confirm zero managed allocations/leases and independent supervision, and record the source, installed runtime, interface, profile, task baseline and budget identities. The installed reasoning/runtime overlay remains byte-identical to the successful microprobe; this change is an operator-only repository registration feature.

The live plan is one unseen offline coding task through authenticated installed Work Mode. Qwen must inspect evidence, author the actual patch, request a passing test, and pass the fresh host evaluator and reviewer to reach COMPLETE. Preserve payload-free receipts and the private patch artifact. Use at most one allocation at USD 2.09/hour, with a 900-second outer allocation limit and USD 0.75 compute ceiling, reserving cleanup time. The unchanged profile permits at most 16 model calls and iterations, 100,000 tokens, 900 inference seconds, USD 1.50 model-accounted cost, and requires review; the outer allocation limit is stricter. Verify the exact serving runtime before any inference. Do not tune prompts, seed a solution, change tests, retry a failed task or run qualification in this authorization. On failure preserve evidence and stop inference; retain the same allocation only for bounded non-inference diagnosis before confirmed cleanup.

Validation and live results are recorded separately so this source freeze is not rewritten after execution.
