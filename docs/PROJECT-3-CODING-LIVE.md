# Project 3 real coding test passed

**REAL QWEN CODING TASK COMPLETE — QUALIFICATION NOT RUN — PROJECT 3 REMAINS UNACCEPTED**

On 2026-09-17 UTC, installed Work Mode task `a031f56ef6be8b09bc1da27bff529554` completed with `COMPLETE / HOST_VALIDATED_FINAL`. This observation report is outside the completed source freeze. It does not change the live-tested code, profile, configuration, prompts, model settings or runtime. The owner requested the coding test and subsequently authorized continued repair without the earlier short time/spending constraint. The operator bounded this allocation to one hour, USD 2.50 and USD 2.09/hour, with a 120-second cleanup reserve. No qualification suite was run under this coding-only operator.

## Observed coding progression

The original synthetic interval-union repository, baseline commit, goal, tests and package remained unchanged. No host-authored solution or patch was seeded. Qwen used the actual authenticated installed Work Mode path:

1. Listed the isolated repository and read three files.
2. Proposed one substantive patch; the Mac authorized it and Git applied it on the first attempt.
3. Requested the explicit test operation. The unchanged test command completed successfully.
4. Received a fresh passing host evaluator, including repeated tests, stable nonempty diff and changed-worktree evidence.
5. Received ACCEPT from the normal separate reviewer call.
6. Reached genuine host-validated COMPLETE through the existing passing-test completion path.

Only index.js changed. The preserved Qwen patch is 701 bytes. Test and package hashes match the original baseline. The original registered repository remains clean; the isolated task workspace was cleaned. Patch artifacts are private task artifacts, not audit-receipt payloads.

There were six work proposals and one reviewer call, seven total reasoner/reviewer inferences. The task summary's six coordinator calls excludes the separate reviewer; transport telemetry correctly records seven. These counts do not include ordinary lifecycle readiness smokes. The earlier diagnostic microprobe's one-readiness/five-proposal budget does not describe this ordinary coding task.

All seven streams completed with stop, parsed successfully and retained unchanged model output. All six proposals were valid. All six authority and result-egress decisions allowed the exact bound action; reviewer egress also allowed. There were zero protocol diagnostics, schema corrections, patch rejections, uncertain effects, escalations or budget stops. The 58-event hash chain and final summary digest reconcile. Task execution took 39.886 seconds; measured proposal/reviewer inference time was 12.761 seconds. The fresh evaluator and reviewer receipts establish the live completion; prior post-task diagnostic tests are not substituted for this evidence.

## Fix and limits of the conclusion

The successful revision removes one redundant JSON encoding layer from nested user-message data before rendering the actual model input. Repository strings retain their original values; strict malformed/duplicate-key/nonobject content remains opaque. It does not promote nested roles, modify the signed request, normalize model intent or relax Mac validation. See [the reviewed presentation change](PROJECT-3-CODING-RENDER.md).

This clean run followed the presentation fix. That is evidence the revised path works on this task; it is not an isolated causal experiment proving encoding was the sole cause of every earlier failure. The earlier general decision guidance and generation field-order fix remain installed. Model/revision, vLLM, compiler, tokenizer/template, quantization, thinking/sampling, output ceiling, sandbox, authority, egress, evaluator and correction limit remain pinned.

The regex for escalation reasons validates syntax only. A valid model-selected ESCALATION stops the task; it is not a prerequisite or permission unlock. The newly added reason-concept regexes are payload-free diagnostic metadata and do not control routing or execution. This successful run required no escalation.

## Preserved earlier results

| Attempt | Live result | Evidence distinction |
| --- | --- | --- |
| Original coding budget | ITERATION_LIMIT after 16 proposals | Final patch passed only in a later local diagnosis; the live task did not complete. |
| Verification-only continuation | Valid ESCALATION after one proposal | A preceding capability-manifest preflight failure used zero inference. No live evaluator/reviewer success; not an acceptance run. |
| Expanded coding budget | BLOCKED / MODEL_ESCALATION after 19 proposals | Final patch passed two of five tests in later diagnosis. |
| Restored general guidance | ITERATION_LIMIT after 32 proposals | Final patch passed four of five tests in later diagnosis. |
| Single-encoding presentation | COMPLETE, six proposals plus reviewer | First patch applied, explicit test passed, fresh evaluator passed, reviewer ACCEPT. |

Each coding rerun started from the unchanged original baseline. Failed tasks retain their original status. The 32-call budget alone did not solve the problem; the successful run fit within the original 16-call ceiling. No qualification fixtures were changed or used as prompt examples. The earlier three failed coding tasks and continuation shared one allocation with retained supervision across in-place amendments. That allocation was eventually cleaned after 2449.686 seconds, estimated USD 1.42218; persistent storage was preserved. The final presentation run used one subsequent allocation. Combined estimated compute for these coding allocations is USD 1.63115, excluding earlier microprobes and persistent storage; these are conservative estimates, not settled invoices.

## Validation and installed identity

All 441 offline tests passed: 112 gate JavaScript, 245 gate Python, 31 reliability JavaScript, 11 reliability Python, 8 MCP, 23 release and 11 plugin tests. No skips or failures remain. The first render-change run exposed three assertions that still parsed the old double-encoded representation; those transmission-boundary assertions were updated while retaining their semantic checks, then the complete suite passed. Build, audit, complete manifest verification, tracked diff whitespace and untracked-text whitespace checks passed. Dependencies were unchanged; the inspected existing Python 3.12 environment and prior cache-only dependency validation were retained.

The supported stopped-gateway work_host amendment is `1789628633552146000`. Its private rollback transaction is preserved. All 50 installed overlay files match the rendered reviewed source; FREEZE and receipt integrity passed. Gateway doctor passed before and after the run. Source and installed identities, profile and baseline were rechecked unchanged after cleanup.

Source-manifest SHA-256: `daed359cb26f5ec06aeed4227a72da2a203a8939c967ee6e87c5240292e83b4e`.

gate/FREEZE.json SHA-256: `904b16e138693ff32e19785f7e511772ca74474b2e176c9d72c5586c029761cc`.

receipt.json SHA-256: `a1881d4197a3cbed685e1e5ea8ebe67896d943f77c3b76b8f4e053be3704722f`.

config/work-mode.json SHA-256: `85b4ceeba9fa6aa4136b0d984fe9bbed1aeb5c976f0d7a780a59a45deb63106e`.

Exact serving proof SHA-256: `a80f69974ea55bda8aef17d8a707a0609e6a9f37bd1eb0eeb4a5d7ba9d7b1453`.

Task audit SHA-256: `ebdc5183cc2d040e4a00edb5d29ec12a93cd3e0b8653031b2759521c01059fe1`.

Cleanup proof SHA-256: `c437db550f4babc63b1e6160ab5f4ea028c10664864e6451f07b8d3ea3f052e8`.

Exact non-inference serving verification passed before the first readiness inference: pinned Qwen checkpoint/revision, vLLM 0.20.1, auto-selected xgrammar environment, tokenizer/template identity, stable process, all six production generation schemas and 25 representative branches with fresh matcher state, EOS acceptance, and negative controls. Full host semantic validation remains authoritative. All rendered token measurements fit the unchanged 32768-token window, with 4096-token reserve and 1024-token proposal ceiling.

| Production request | Rendered input tokens | Remaining headroom |
| --- | ---: | ---: |
| ordinaryInitial | 1304 | 27368 |
| accumulatedObservations | 4616 | 24056 |
| activeCorrection | 4819 | 23853 |
| nearCharacterLimit | 9100 | 19572 |
| postPatchTestOnly | 935 | 27737 |
| reviewer | 621 | 28051 |

## Cleanup and remaining gates

The final allocation was provider-confirmed absent after 359.948 seconds, estimated USD 0.20897 at USD 2.09/hour. A fresh independent inventory check found zero managed allocations, zero private leases, no unresolved experiment and no uncertain ownership. PRIVATE_LEAD is OFFLINE; PRIVATE_80B remains permanently RETIRED and reconciled. Both GPU autostarts remain off, the independent janitor remains loaded, and persistent storage is preserved. The candidate gateway remains healthy.

The branch remains v1.1/project-3-private-lead-workmode at e168f864eb5ed74d3437102323cdf79804505f06 with the intentional uncommitted repair tree and empty index. No commit, push, PR, merge, promotion or legacy-tree modification occurred.

This establishes the real coding success gate on the final installed revision. The unchanged 11-case qualification and fresh independent Project 3 acceptance audit remain outstanding. No further runtime/source fix was needed after the successful task.

Subsequent observation: the owner-authorized [unchanged qualification run](PROJECT-3-QUALIFICATION-LIVE.md) reached all eleven expected harness outcomes but exposed a test-weakening finding. See that report for the current remaining gate. This does not alter the coding-run evidence above.
