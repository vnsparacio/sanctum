# Project 3 model-facing message presentation

The guidance-restored run also reached its 32-step budget without completion. Its final preserved implementation passed four of five unchanged tests; the remaining failure was input/output array aliasing. Repeated raw Git patch rejections, rather than reason-code validation, dominated the run. This result is preserved and remains a failed coding test. No qualification ran. The extended supervisor subsequently completed cleanup; fresh provider inventory and local lease checks confirmed zero managed allocations and leases. The one allocation's conservative total compute estimate was USD 1.42218 over 2449.686 seconds, under the original and owner-extended authorization. Persistent storage was preserved.

Code inspection confirms that Work Mode's user-message content was serialized JSON inside a second serialized request envelope. Thus repository newlines and quotes carried an extra escape layer into the model's visible user message. This is a concrete presentation property; it is not yet proven to be the cause of the model's patch errors.

The change introduces a model-only request view: complete strict JSON objects in nested user-message content are represented as objects before the outer envelope is rendered. It does not recursively interpret repository strings, alter their bytes, modify the signed request, or promote nested roles/instructions into system authority. Malformed, duplicate-key, scalar and array JSON content remain opaque strings. Nested system-message content is untouched and remains data in the one real user message. All actual chat roles, schemas, host bindings, authority, model outputs, model/revision/runtime pins, generation settings, budgets and correction allowance remain unchanged.

Transmission-boundary regressions cover the real outgoing HTTP bytes, preserved schema and essential task/observation/correction content across every production surface, unchanged source-byte values and signed input, untrusted role-like repository content, and opaque malformed/duplicate-key content. These are presentation tests, not evidence of model improvement.

This is an explicit new source freeze for backends.py, its protocol tests and this report only. Before-copies are external. Use the supported stopped-gateway host amendment, then exact runtime/schema/token verification before another live inference. Preserve prior failures; do not seed a solution or change the unseen baseline, tests, goal, or qualification fixtures. Live results are recorded separately.
