# Project 3 generation-schema compatibility repair

The owner explicitly approved omitting broken string-length constraints only from the GPU generation schema, retaining all length/path rules and full semantic validation on the Mac, and requiring GPU compilation of the generation schema rather than the host semantic schema. This supersedes the earlier dual-compiler requirement for this narrow compatibility issue. No additional GPU allocation was authorized by that approval.

The plan is to project only the incompatible string constraints, demonstrate unchanged host enforcement and acceptance of valid escaped JSON, validate and review the complete source package, then install through the supported stopped-gateway amendment. A separately authorized bounded allocation is still required for live confirmation. The preceding failure and receipts remain documented in PROJECT-3-QWEN-COMPILER-COMPATIBILITY.md.

## Change and review scope

Starting branch is `v1.1/project-3-private-lead-workmode`, HEAD `e168f864eb5ed74d3437102323cdf79804505f06`, starting source-manifest SHA-256 `5a1f69c5b40b78668d2059e839dd4f5fb3874d0af9c36c7febe0abd1f12662a2`. The existing uncommitted tree and empty index are preserved. Before-copies and private evidence remain outside source. No commits, branch switches, pushes, PRs, merges or legacy-tree changes belong to this repair.

- `gate/foundation/vllm-structured-output.mjs` omits minLength/maxLength only on string nodes in the generation projection and records a specific omission reason. Validation rejects generation schemas that still carry these incompatible constraints. Existing pattern projection, all other keywords, the historical dialect identifier and runtime pins are preserved.
- `gate/runtime-readiness.mjs` runs branch-specific authoritative Mac validation for all representatives and generates negative controls for unknown fields, string types, present length constraints and unsafe paths. Root oneOf is checked explicitly rather than assumed to be handled by the branch matcher. The resulting host evidence binds the full semantic schema and representative digests.
- `gate/verify_exact_runtime.py` requires successful bound host semantic evidence before inspecting the runtime. It compiles each whole production generation schema unchanged, with a fresh grammar per representative through EOS, and reports semantic compilation as not required because the Mac is authoritative. Missing, failed or unbound host evidence refuses. Expected environment identity still binds the complete artifact digest. No compiler success is claimed for the host schema.
- `gate/tests/prelive.test.mjs`, `gate/tests/test_prelive.py`, `gate/tests/test_prelive_targeted.py` `gate/tests/protocol-repair.test.mjs` and `gate/tests/work-mode.test.mjs` verify projection, preserved rejection, evidence binding and the revised compiler contract. Two terminal differential expectations now correctly permit generation while continuing to require host rejection; no rejection assertion was removed.
- This report and only the explained source-manifest entries complete the new source freeze.

Authoritative schema construction, semantic enforcement, path/length rules, plugin capability pins, egress, evaluator, signed authority, sandbox, correction allowance, token budgets, allocation ownership and cleanup are unchanged. Qwen revision, vLLM 0.20.1, auto backend selection, quantization, attention, MoE, KV, thinking and sampling configuration are unchanged. PRIVATE_80B stays permanently retired and both GPU autostarts stay off.

## Evidence and limits

The actual local native xgrammar 0.2.5.post1 compiler, with versions constrained to the observed serving inventory and the pinned tokenizer bytes, accepts all 25 representatives through EOS across all six complete production generation schemas. It rejects an invalid object on each surface. The semantic schema for every surface is byte-equivalent as canonical JSON to the prior installed artifact. All 99 host negative controls reject. Local native platform bytes differ from Linux; this is compiler reproduction evidence, not fresh proof from a serving allocation. No weights were loaded and no model calls occurred.

Pinned tokenizer measurements for ordinary initial, accumulated observations, correction, near character limit, post-patch test and reviewer requests are respectively 1239, 4574, 4791, 9036, 858 and 535 input tokens. Headrooms are 27433, 24098, 23881, 19636, 27814 and 28137 with the unchanged 32768 window and 4096 reserve. System content uses 77 tokens and chat overhead uses 17 for each. The actual rendered IDs are checked against independent encoding of the rendered text.

Focused Node protocol/readiness tests and 70 Python pre-live tests pass. Package closure comprises 50 overlay files, 57 JavaScript edges and 73 Python edges. The first full run identified one old projection expectation that still required generation path lengths. It was updated to require those exact limits on the authoritative schema, omit them only from generation, and additionally reject an empty path on the Mac. The complete suite passes **431 tests**: gate Node 111, gate Python 241, reliability Node 31, reliability Python 11, MCP 8, release Python 18 and plugin tests 11; zero failures, errors or skips. Focused checks are not counted twice. Cache-only `make deps` passed after inspecting the existing Python 3.12.14 environment. `make build` passed all six plugin builds; `make audit` found zero issues in 291 source files. No implementation change followed the successful full suite. This is implementing-session review, not independent Project 3 acceptance.

## Installation, rollback and live gate

Install only after complete validation and an explicit new source freeze, using the supported reversible amendment with the gateway stopped and confirmed zero ownership. Verify every rendered overlay file, installed freeze, receipt, runtime pins, doctor and authenticated help/status afterward. Preserve the prior amendment for rollback and all existing evidence/volumes. Source rollback restores only this repair's exact before-copies, never the larger working tree.

A future allocation needs fresh owner authorization. Retain that allocation for bounded non-inference diagnosis and verifier rechecks on a recoverable failure, honoring the owner's instruction to fix in place. Preserve the same absolute 900-second deadline, USD 0.75 ceiling, at-most-USD-2.09/hour quote, one allocation, one readiness call up to 16 tokens and five proposals up to 1024 tokens. Reserve 120 seconds for cleanup. No inference may run before exact serving verification passes; source/install identity changes invalidate the existing binding. Run only inspection, host-seeded post-patch test and inability microprobes, stopping inference at the first failed probe. No replacement allocation, budget refill, real coding or qualification is included.
