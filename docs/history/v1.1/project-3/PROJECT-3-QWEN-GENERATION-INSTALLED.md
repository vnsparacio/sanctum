# Project 3 generation compatibility: installed checks

The owner-approved [generation compatibility repair](PROJECT-3-QWEN-GENERATION-COMPATIBILITY.md) is installed. This observation report is outside the completed source freeze. The repair is locally verified; fresh serving verification and the three live microprobes remain pending a separately authorized allocation.

Source-manifest SHA-256: `fb6eceb7458c29781de17265a31b5ca1909ac0b31a21b449dc3bab8827ad49cf`.
Installed FREEZE SHA-256: `535eff211a5189adeb028502b67577d5f3dec65683ee5f739788e7ce3e6a8f42`.
Receipt SHA-256: `7df353bac599f0ee5c680fd387e5a5b86447a530b15ab9a82c1eb692ef8c621e`.
Reversible amendment: `work-mode-1789621559404623000`.
Transaction SHA-256: `72a242598ec37fededeae670424db3ecf7026247ea4df192668a362e84b8d4c4`.
Runtime artifact SHA-256: `13b34ad574d42fda43fd006d653a64f67cb7b3105e1dba72e1df7382ea2eea2b`.

All 50 rendered overlay files match the reviewed source, all 68 installed frozen files match their digests, and the receipt and runtime pins verify. Source and installed readiness artifacts are identical. Doctor passed while stopped and after restart; the authenticated loopback gateway has a matching identity and healthy socket. Authenticated `/work help`, `/gate new`, `/gate status` and permanent `/gate include 80b` refusal passed. Both GPU autostarts remain off.

An installed metadata-only experiment observed a fresh independent supervisor heartbeat, zero allocation attempts and zero inference reservations, then reconciled COMPLETE / CONFIRMED. A fresh provider inventory found zero managed allocations; the current lead quote was USD 2.09/hour and available. PRIVATE_80B remains RETIRED and PRIVATE_LEAD OFFLINE, with zero leases and unresolved experiments. Persistent volumes and prior receipts are preserved.

The prepared private live operator binds this source, install, artifact and its own bytes to a fresh explicit authorization. It refuses without authorization. On exact-verifier failure it retains the same allocation for non-inference diagnosis and rechecks, restoring the reviewed verifier before a recheck can open inference. The existing absolute deadline and 120-second cleanup reserve remain enforced by the installed ledger and supervisor. It exposes no additional inference retry or replacement-allocation path. The fixed signed probe caller is unchanged.

No new allocation or model call occurred during this repair. The two prior authorizations are consumed. The compatibility approval expressly authorized no new allocation. A fresh owner decision is therefore required for the prepared live check, at one allocation, USD 0.75 total ceiling, at most 900 seconds and USD 2.09/hour, one readiness call up to 16 tokens and five proposals up to 1024 tokens, only inspection → post-patch test → inability. Real coding and qualification remain separate later gates.
