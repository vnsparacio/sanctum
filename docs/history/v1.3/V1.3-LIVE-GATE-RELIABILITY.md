# V1.3 live gate reliability repair

## Observed failures and scope

On 2026-09-24, a saved owner WebUI chat returned grounding failures for both ordinary and strong 94114 weather requests. A separate request for the most recent email tried Gmail twice, received broker failures, then used Messages tools and delivered an unrelated iMessage answer. The earlier Source-First ranking-only change did not qualify these real workflows. No personal message contents are included in this source report.

The isolated Google credential store had an OAuth token but no default client credential. After the owner created a replacement secret, the client JSON was imported into the private `gog` home. `gog auth doctor` and read-only Gmail-label and Calendar-list API checks passed. This proves direct Google access, not a gate conversation or broker-driven message read. Credential rotation remains a separate owner checkpoint because the newly generated secret appeared in an automation diagnostic trace; neither the prior secret nor the new one was disabled here.

## Reviewed repair

- For a non-private public weather request with exactly one explicitly supplied five-digit ZIP, Mac query minimization retains that ZIP. Numeric identifiers in other contexts and ZIPs in private context are not automatically sent to public search. Explicit local-source recency (such as “my latest email”) does not trigger public web retrieval.
- The Source-First coordinator rejects ZIP-weather search candidates whose URL or title does not identify that ZIP. It marks the pack adequate only after fetching weather facts, rather than treating any nonempty unrelated page as sufficient.
- The local OpenClaw handoff partitions explicit Gmail, Messages and Calendar requests by source family. A prompt-build tool cap and pre-tool guard prevent wrong-family calls. A personal-source final answer is suppressed when no requested-family tool call succeeds. Source-First evidence answers have no optional tools. This is a fail-closed boundary, not a new permission or provider fallback.
- A session contract test checks that one eligible Gemini audit grant spans `ask` and `ask-strong`, while the frontier answer still requires its own exact approval. This documents the distinction between the audit session and answer disclosure; no hosted answer approval was removed.

## Validation and limits

Focused Source-First, local-agent and Mac policy tests pass with synthetic fixtures. Full `make deps`, `make build`, `make test` and `make audit` pass with the explicit source freeze. A separate bounded live probe through the running public web provider fetched three ZIP-matching weather pages, including an NWS forecast; the running local 4B agent returned a valid grounded-answer object against the new Source-First evidence pack. This was a controlled local-agent probe, not a saved WebUI gate conversation or installed-source acceptance. Run `make doctor` after the supported private amendment.

## Post-merge live acceptance, September 24

After PR #98 merged, the reviewed Source-First amendment and doctor passed, and the managed stack restarted. A saved WebUI chat confirmed that a Gemini audit session grant was reused from ordinary `ask` to `ask-strong`; the strong answer still requested its separate one-time frontier approval. A separate saved chat answered a latest-email request from Gmail without falling through to Messages. The ordinary weather request did not fail validation, but it delivered a grounded statement of missing forecast information because the only fetched page was Weather Channel promotional text. The strong weather request failed grounding. Neither is a qualified weather answer.

The live Parallel search for the verbose minimized query returned no NWS result, whereas a short `94114 weather forecast today` query returned a ZIP-specific NWS result. Core fetch of that NWS result yielded numeric temperature and wind facts; core fetch of the Weather Channel result yielded only forecaster marketing copy. This follow-up change shortens the current-day ZIP query, prefers authoritative candidates, and requires numeric weather facts instead of the word `forecast` alone. These observations are bounded live diagnostics; final saved-chat acceptance remains required after this follow-up is reviewed, merged, and installed.

After PR #99 merged and its stopped-gateway amendment passed doctor, a fresh saved chat returned a cited ordinary NWS forecast. Its local answer nevertheless inferred a low precipitation chance not numerically stated by the fetched source. The original full `ask-strong` request again failed `GROUNDING_REQUIRED`; a narrower strong request limited to conditions, temperature, and wind returned a cited frontier answer. The owner confirmed clicking the fresh disclosure dialog for that diagnostic answer and a later independent frontier check. This is evidence of an answer-completeness gap, not a reason to weaken the host citation or grounding checks. The next reviewed prompt change directs both answer paths to state absent fields as unavailable and keep every affirmative claim source-supported. It still needs installed saved-chat acceptance.

Do not call this live-qualified until the owner reviews and merges the feature PR, the stopped-gateway Source-First amendment is applied, and fresh saved-chat tests show: one session audit approval, a grounded ordinary weather answer, a grounded strong weather answer after exact frontier approval, and an email request that uses only Gmail or fails closed. The real model and live search provider may still reveal quality gaps. Preserve the existing runtime and rollback receipt until those checks pass.

## Exact Messages sender follow-up

After PR #100 was installed, a saved ordinary weather answer cited NWS conditions, temperature and wind and stated that precipitation probability was absent; the owner separately verified weather behavior. A later owner request for the last ten messages *from* a phone number instead received a speculative privacy/access explanation. The read-only Messages broker was healthy, and that number appeared as a participant in recent group chats, but the existing model tool could only search message bodies, resolve preconfigured contact aliases, or read one known chat. It could not distinguish one sender across chats. No private number or message text is retained in this source record.

This follow-up adds an exact E.164 sender path to the existing configured Messages search capability. The user-only broker queries inbound ordinary messages by exact `handle.id`, omits reactions and system records, caps the result at twelve and labels unavailable or truncated content. Unknown numbers return no records; malformed numbers fail without a broader search. Synthetic fixtures cover exact identity, cross-chat ordering, absent text and bounds. A direct, synthetic prompt to the already-running local 4B model selected `messages_search` with `from:+E164` and limit ten; that checks tool selection, not the installed gate or message answer. The new path has not been installed or live-qualified by this source change alone; post-merge acceptance requires a stopped-stack update and a saved-chat test of the original sender request without exposing message contents in source or logs.
