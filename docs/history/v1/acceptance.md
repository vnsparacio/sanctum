# Candidate acceptance — 2026-09-11

This is the original live-qualification record, retained as evidence. The public Sanctum rename, current source/test counts and publication checks are recorded in [publication](publication.md). The original 174-file / 151-test figures are historical, not the renamed-tree totals.
**READY WITH DOCUMENTED EXCEPTIONS.** Documented exceptions and exact live-test scope are in [qualification](qualification.md).

## Reproducible checks

The final suite passed **151 tests** in both the candidate and the new source-only copy: gate 93 (64 Python, 29 Node), reliability 29 (11 Python, 18 Node), MCP 8, packaging 10 and plugins 11. Canonical builds validate all six plugin packages and reviewed runtime pins. The browser-session regression prevents implicit WebUI built-in tools from reaching the gate while retaining explicit-tool rejection. The added MCP regression verifies that an operator-selected private input directory replaces repository fixtures; the guard still rejects symlinks and escapes.

The release record includes candidate build/tests plus a new source-only copy with fresh npm/Python installation, build, the same full suite, source audit, isolated setup/doctor and Compose validation. Downloads use pinned dependencies and available caches. No new multi-GB model-weight download or Linux CI run is claimed.

## Live evidence

- Fresh prefix-owned MLX/gateway and isolated WebUI application paths passed, including no-fallback failure behavior. Interactive sign-in, help/new, exclusions, exact disclosure, local arithmetic, status, cancel and end now also passed.
- Synthetic hosted routes passed; prior accounting recorded 15 received network calls totaling $0.22470818. Two later, explicitly bounded prompt-only audits added $0.015442; their temporary provider profile was removed. This is adapter accounting, not an independent invoice.
- Explicitly authorized Calendar/Gmail reads and local summary/draft generation passed; factual answer quality was not independently audited. Calendar broker output caps and write denial passed. Messages first failed in the app context, then the owner-run native helper read at most three matching records and generated a local summary and labelled unsent draft. A final owner-run test also passed the actual candidate broker and wrapper with returned limit one, which fetches at most three raw records; mutation methods and send were denied, and the generated supporting quote matched the source. No raw personal-source payloads were retained.
- File inventory/inspect/create, denied move, one-use approved move/rename/undo and approval replay denial passed through the initialized pinned OpenClaw runtime and live isolated brokers. Markdown notes/email drafts/message drafts were created only from synthetic data; same-title creation did not overwrite.
- Browser Guard passed real navigation/snapshot and denied/approved harmless clicks through the native tool and approval runtime. Evaluation and other-profile requests were denied; the test browser stopped afterward. The local model separately selected an unavailable sandbox target, a retained failure. Existing time, Hugging Face public search and offline Markdown MCP transports passed after Docker startup; the configured input mount matched the guard.
- The independent login janitor performed a real sweep and another after restart. The bounded private-80B signed-worker test returned two correct answers on one Pod, verified model/loopback/tunnel identity, and confirmed provider-side deletion with zero leases. GPU autostart was disabled afterward.

## Publication and preservation

The allowlisted source tree contains 174 files. The final audit checks selected secret/private-artifact patterns and concrete owner/account/resource/test-credential bindings; no raw personal fixtures, runtime state, credentials or logs belong in the archive. Matching Git/Docker ignores exclude private runtimes. No Git history exists to inspect. Original copied inputs and production plugin links are rechecked before packaging.

Earlier disk-full, launchd, stopped-Docker and harness failures are retained in private evidence and explained in the qualification report. Final artifacts do not erase those failures. No production source, service configuration, model cache or provider volume was replaced. The new archive is a review candidate, not an approved v1.0.0 release.

Final verification: all nine fresh-copy stages passed, all six canonical plugin builds passed, and the 174-file source/contextual scan had zero findings. All 125 original copied inputs are unchanged; production retains ten original plugin paths and no candidate links. Staged Google client and temporary hosted-profile credentials are removed, and GPU deletion was provider-confirmed with zero leases before test janitor removal.

The final candidate Messages helper passed the actual broker/wrapper read bound, four HTTP write denials, unavailable send command, structured summary/draft generation and exact supporting-source quote. Its literal DRAFT-marker assertion failed. This formatting failure remains a documented exception; the original result is preserved rather than relabelled as a fully passing script.

Final cleanup confirmed no candidate MLX, WebUI, gateway or test-browser listeners, no GPU ownership, and no temporary hosted-provider profile. The owner-run Messages test socket was removed. Production plugin links and original input hashes remain unchanged.
