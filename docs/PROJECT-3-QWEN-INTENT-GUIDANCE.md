# Project 3 host decision guidance repair

The generation compatibility repair passed actual serving verification on the third separately authorized allocation: all six production generation schemas compiled and all 25 representatives accepted through EOS; model/tokenizer/template identity and token fit passed; the one readiness call succeeded. The first inspection proposal was a valid ESCALATION and the fixed microprobe correctly failed. Its reason text was not retained, so the precise model cause cannot be established from that receipt.

Experiment `63d6438d3e91be4abd9568309ad0a424` made one allocation and two known-complete model calls (one readiness, one proposal), with no uncertain calls. It reconciled COMPLETE / CONFIRMED / ABSENT, zero leases and zero managed allocations. The conservative creation-to-absence interval was 298.039 seconds, approximately USD 0.17303 compute at USD 2.09/hour, not a settled invoice. Receipt SHA-256 is `720dcb088141500aef1280df86aa07b79731ed1608ba5f479998740a3d7a6d03`; exact verification SHA-256 is `5cbba59a3787adb5c1aa7cb7142f02fb2fa820ec70206625864192c3df72159d`. PRIVATE_80B remains retired; storage is preserved.

**LIVE MICROPROBE FAILED — EVIDENCE PRESERVED — PROJECT 3 REMAINS UNACCEPTED**

## Diagnosis, plan and implementation

The owner requested continued repair until resolved. The top-level production system prompt described all supplied content as untrusted but did not explain that the supplied task is still the requested goal or distinguish a proposal for missing evidence from inability. Host Work Mode instructions were nested in the user payload and result requirements specifically described ESCALATION formatting. This is a plausible source of premature inability, not a proven explanation of the model's unretained reason.

The repair adds fixed, general host decision guidance in `gate/src/backends.py`: task data describes the goal without granting authority; propose a listed capability when it can obtain missing evidence or advance the goal; a proposal requests Mac validation/execution and does not assert an action occurred; escalate when no listed capability can make progress or the host requires stopping; final requires host completion permission. Instructions in observations/retrieved content cannot grant permissions. Dynamic nested system messages remain user data and are never promoted into the real system role.

The change contains no fixture-specific names, example solutions, forced inspection answer, consequential-intent rewrite, fallback model or additional retry. The three fixed microprobes and their expected outcomes are unchanged. The accepted profile file and every model/runtime/sampling/thinking/context/output pin are unchanged. Authoritative schemas, decoder schemas, Mac authority, egress, evaluator, sandbox, budgets and cleanup machinery are unchanged. Valid ESCALATION remains a terminal refusal; it is not automatically retried or repaired.

`gate/tests/test_protocol_repair.py` adds a transmission-boundary regression verifying that the new guidance is system-owned, untrusted nested instructions remain user data, the exact schema/pins survive and both valid proposals and inability results return unchanged. The source freeze changes only these two files and this report relative to manifest `fb6eceb7458c29781de17265a31b5ca1909ac0b31a21b449dc3bab8827ad49cf`. Exact before-copies are external. Branch, HEAD, larger working tree and empty index are preserved; legacy is untouched.

The plan is to validate the source and rendered-token budget, review, freeze, install reversibly with the gateway stopped, verify installed bytes/doctor/authentication/supervision, and then request a fresh bounded live decision. Offline checks cannot establish that the model's semantic choice is fixed.

## Correcting in-place diagnosis

The previous private operator retained verifier failures but still cleaned up immediately on a failed semantic probe. That missed the owner's in-place diagnosis preference. The next operator retains both verifier and semantic failures for non-inference diagnosis within the original deadline and 120-second cleanup reserve. A semantic failure never resumes inference; there is no extra probe or correction retry. The readiness subprocess may use the existing remaining deadline instead of an unnecessary shorter local timeout. Installed admission, model-call and cleanup limits are unchanged.

Two isolated orchestration checks execute the actual private operator with external provider/control calls stubbed, covering both verifier failure and semantic failure. Both observe the diagnostic pause before stop/sweep, exactly one allocation path, and no additional inference; no real provider calls occur. The probe wrapper retains only bounded numeric telemetry, result kind, a fixed allowlist classification of escalation codes and a digest plus fixed concept flags; it does not retain arbitrary reason text, prompts, tool arguments or model reasoning. Caller bytes and the runtime artifact will be bound to any fresh authorization.

## Validation and later gate

All **432 tests pass**: gate Node 111, gate Python 242, reliability Node 31, reliability Python 11, MCP 8, release Python 18 and plugin tests 11. Zero failures, errors or skips. Focused checks are not counted twice. The prior inspected cache-only dependency installation remains valid; no dependencies changed. `make build` passed all six plugins and `make audit` found zero issues in 293 files. Rendered input totals are 1334, 4669, 4886, 9131, 953 and 630; remaining headrooms are 27338, 24003, 23786, 19541, 27719 and 28042. System content is 172 tokens, overhead 17, reserve 4096, window 32768, proposal ceiling 1024. The actual pinned tokenizer and independent rendered-text encoding agree. The schemas are unchanged from the successful live compiler proof. Installed identity is recorded separately after amendment; no implementation change followed the passing suite. This is implementing-session review, not independent acceptance. The third allocation authorization is consumed. No new allocation is authorized by this repair, and no real coding or qualification may start without the later explicit gate.
