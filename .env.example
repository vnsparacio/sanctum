# No API keys are required for offline tests.
# Operator paths are generated by make setup into PREFIX/config/environment.json.
# Keep credentials in owner-only local stores. Never paste tokens here.
